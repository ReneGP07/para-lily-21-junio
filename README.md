# Carta interactiva para Lily

Versión v13 estable.

Corrección:

- La lógica de los botones ahora está integrada directamente en `index.html`.
- Ya no depende de `script.js`, así se evita que falle por caché o por no cargar el archivo externo.
- Se mantuvo la elección de música al inicio.
- El botón `Ver collage` está separado de las 29 fotos.
- El botón `Siguiente` debajo del collage abre el marco final.

## Archivos a subir a GitHub

Sube y reemplaza:

```text
index.html
style.css
script.js
README.md
assets
```

Luego prueba con:

```text
?v=13
```

Si lo abres local, extrae primero el ZIP completo y abre `index.html`.
No abras el archivo desde dentro del ZIP.
